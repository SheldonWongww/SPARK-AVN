# Legacy ENMuS metrics

The JSON files in this directory predate the run-manifest convention. They are retained as historical evidence only. Their checkpoint identity, seed, exact command, code commit, and dataset version are incomplete, so they must not populate the formal comparison table.
